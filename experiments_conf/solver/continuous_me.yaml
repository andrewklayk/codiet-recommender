name: continuous_me
model_name: CONT
custom_objective: none
n_runs: 5
recalculate_dag: false

# neural network architecture
# backbone: mlp | deep_mlp | transformer
network: mlp
hidden_dim: 64
n_layers: 2

# training
n_epochs: 50
batch_size: 256      # equal across ALL continuous variants: the
                     # partial-correlation constraint estimator needs a
                     # large batch, so every arm gets the same one
learning_rate: 0.001

# interaction-graph baselines: restrict the model's input features.
restrict_to_parents: false
restrict_to_markov_blanket: false

# continuous constraints-function knobs (see ContinuousTripletConstraintsFn)
cond_basis: linear
ridge: 0.0001
conditioner: linear

# Moreau-envelope smoothing ALONE, no ALM duals/constraints: isolates the
# effect of the optimizer from the effect of the causal constraints (use_alm
# stays false here -- this is the "uncon_ME" ablation).
use_alm: false
use_moreau: true
moreau_mu: 2.0
moreau_beta: 0.5

# debugging
debug: false
